// a shared functionality between fe and be
// example message format {type: updateBoardTitle, value: string}
//config could also have some constraints set up, ie minLength maxLength everything to do with msg processing